<h1>Subjects#show</h1>
<p>Find me in app/views/subjects/show.html.erb</p>





<h1 class="text-3xl font-bold mb-4"><%= @subject.title %></h1>
<p class="mb-6 text-gray-700"><%= @subject.description %></p>

<h2 class="text-2xl font-semibold mt-8 mb-2">📖 Versets associés :</h2>
<ul class="space-y-4">
  <% @subject.verses.each do |verse| %>
    <li class="bg-gray-100 p-4 rounded shadow">
      <strong>Sourate <%= verse.surah_number %>, Verset <%= verse.verse_number %> :</strong>
      <p class="mt-1 text-gray-800"><%= verse.text %></p>
    </li>
  <% end %>
</ul>
