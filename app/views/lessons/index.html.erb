<!-- <p style="color: green"><%= notice %></p>

<% content_for :title, "Lessons" %>

<h1>Lessons</h1>

<div id="lessons">
  <% @lessons.each do |lesson| %>
    <%= render lesson %>
    <p>
      <%= link_to "Show this lesson", lesson %>
    </p>
  <% end %>
</div>

<%= link_to "New lesson", new_lesson_path %> -->

<h1 class="mt-8 text-3xl font-bold mb-6">📚 Liste des leçons</h1>

<%= link_to "➕ Nouvelle leçon", new_lesson_path, class: "bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-700 mb-4 inline-block" %>

<div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
  <% @lessons.each do |lesson| %>
    <div class="bg-white p-4 shadow rounded-lg border border-gray-200">
      <h2 class="text-xl font-semibold"><%= lesson.title %></h2>
      <p class="text-gray-600 mt-2"><%= truncate(lesson.content, length: 100) %></p>
      <div class="mt-4 flex justify-between">
        <%= link_to "Voir", lesson_path(lesson), class: "text-blue-600 hover:underline" %>
        <%= link_to "Modifier", edit_lesson_path(lesson), class: "text-yellow-600 hover:underline" %>
        <%= link_to "Supprimer", lesson, method: :delete, data: { confirm: "Es-tu sûr ?" }, class: "text-red-600 hover:underline" %>
      </div>
    </div>
  <% end %>
</div>
